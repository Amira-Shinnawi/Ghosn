import 'package:flutter/material.dart';

class Paypal_body extends StatelessWidget {
  const Paypal_body({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:Text("PAYPAL") ,
    );
  }
}